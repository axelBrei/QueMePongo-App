<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeWithoutActionBar" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="AppThemeWithoutActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>
    
    <style name="toolbarSpinner" parent="Widget.AppCompat.Spinner.Underlined">
        <item name="android:textColor">@color/colorPrimarytext</item>
    </style>

    <style name="GenericSpinnerStyle" parent="Base.Widget.AppCompat.Spinner.Underlined">
        <item name="android:textColor">@color/colorSecondaryText</item>
    </style>

    <style name="EditText" parent="AppTheme">
        <item name="colorControlNormal">@color/grayFontColor</item>
        <item name="colorControlActivated">@color/colorAccentLight</item>
        <item name="android:textColorPrimary">@color/colorSecondaryText</item>
        <item name="android:colorError" tools:ignore="NewApi">@color/error_color</item>
        <item name="colorError">@color/error_color</item>
    </style>



</resources>
